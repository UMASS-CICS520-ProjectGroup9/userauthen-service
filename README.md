# User Authentication Service

This service handles user authentication for the UMASS project. It is a Django project.

## Getting Started

To run this service:

1.  Install the dependencies:
    ```bash
    pip install -r requirements.txt
    ```
2.  Run the development server:
    ```bash
    cd userauthen
    python manage.py runserver
    ```

